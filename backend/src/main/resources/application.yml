server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/pdf-forms
      database: pdf-forms
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

logging:
  level:
    com.pdfforms: DEBUG
    org.apache.pdfbox: WARN
